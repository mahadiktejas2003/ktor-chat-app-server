package com.abc.room

class MemberAlreadyExistsException:Exception (
    "there is already a member with that username in the room."

    // we actually want to join a room
// and there is already a member with our username in that room we just throw this exception and display corresponding error in the app
)